---
title: "Analyse data Nieuwkapelleplas"
author: "Florian"
date: "`r Sys.Date()`"
output: 
  html_document:
    toc: TRUE
    toc_float:
      collapsed: FALSE
      smooth_scroll: FALSE
editor_options: 
  chunk_output_type: console
---
!! gebruik een project in R; met als submappen /data en /exports

```{r}
# Enable inbo r-universe
options(repos = c(
    inbo = 'https://inbo.r-universe.dev',
    CRAN = 'https://cloud.r-project.org'))

# Install the package
#install.packages('ggpubr')
```


```{r setup, results ='hide', eval = TRUE, echo = FALSE, message = FALSE, cache = FALSE, purl = FALSE, warning = FALSE}
library(knitr)
opts_chunk$set(
  echo = FALSE, 
  eval = TRUE,  
  cache = FALSE, 
  autodep = TRUE,
  dpi = 300,
  fig.width = 150 / 25.4,
  fig.height = 100 / 25.4,
  warning = TRUE,
  error = TRUE,
  message = TRUE
)

library(ggpubr)
library(lubridate)
library(readxl) 
library(tidyverse)
library(ggplot2)
library(dplyr)
library(inbodb)
library(gridExtra)

```

# import
```{r }
# data met manuele toevoeging T.N (som van NO2, NO3, NH4 en _N)
data <- read.csv2("./data/data_NKP.csv", sep = ",")
data$ResultCalc <- as.numeric(data$ResultCalc)
data$FieldSamplingDate <- as.Date(data$FieldSamplingDate, format = "%d/%m/%Y")
# remove 2 rowz of Na with LIMS-analysis name ICP_MET_OPT8300_W
data <- data[-(which(data$AquaComponent %in% "Na" & data$LIMSAnalysisName %in% "ICP_MET_OPT8300_W")),]

data_ZP <- filter(data, data$FieldSampleID == "WVLDIK0133_ZP")
data_R <- filter(data, data$FieldSampleID == "WVLDIK0133_R")
```

# neem gemiddelde waarde tussen randzone en zonnepanelen-zone
```{r}
combitabel <- bind_rows(data_ZP, data_R)
combitabel <- combitabel %>%
  group_by(FieldSamplingDate, AquaComponent, label) %>%
  summarise(combo = mean(ResultCalc)) %>%
  select(FieldSamplingDate, AquaComponent, combo, label)
```


# gemiddeldes, maximum, minimum per variabele per zone
```{r}
# voor zonnepaneelzone (_ZP)
data_ZP_stat <- data_ZP %>%
  group_by(AquaComponent) %>%
  summarise(
    gem = mean(ResultCalc),
    min = min(ResultCalc),
    max = max(ResultCalc),
    p10 = quantile(ResultCalc, probs = 0.1),
    p90 = quantile(ResultCalc, probs = 0.9)
  )

# voor randzone (_R)
data_R_stat <- data_R %>%
  group_by(AquaComponent) %>%
  summarise(
    gem = mean(ResultCalc),
    min = min(ResultCalc),
    max = max(ResultCalc),
    p10 = quantile(ResultCalc, probs = 0.1),
    p90 = quantile(ResultCalc, probs = 0.9)
  )
```

# figuren

per staalnamelocatie: alle gemeten variabelen met aanduiding van MKN richtwaarden (indien opgegeven) in figuur zetten
x = FieldSamplingDate
y = ResultCalc
label: als titel

# vul hier MKN-normen in voor de te onderzoeken stof(fen)
```{r}
## watertype Awe - Nieuwkappelleplas
MKN_O2conc = 6 # mg O2/L - 10p
MKN_02verz = 120 # % - maximum

MKN_TN = 1.3 # mg/L - zomerhalfjaargem
MKN_TP = 0.055 # mg/L - zomerhalfjaargem

MKN_doorzicht = 1.8 # m - zomerhalfjaargem

MKN_EC = 750 # µS/cm (20°C) - 90p
MKN_Cl = 140 # mg/L - gemiddelde
MKN_Sulfaat = 100 # mg/L - gemiddelde

MKN_temp = 25 # °C - maximum

MKN_pH_min = 6.5  # vul minimum pH in
MKN_pH_max = 8.5  # vul maximum pH in
```


```{r}
# figuur met 2 trendlijnen (1 voor ZP, 1 voor R)
figuur <- data %>% 
    filter(AquaComponent == "Cl") %>% 
    ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = FieldSampleID)) + 
    geom_line() +
    geom_point() +
  geom_hline(yintercept = MKN_Cl, colour = "blue", linetype = "dashed") +
  xlab("Datum staalname") + 
  ylab("Chloride (mg/L)") +
    scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  scale_color_discrete(name = "Locatie", labels = c("Randzone", "Zonnepanelen"))
figuur

## pH - watertype Awe (met minimum en maximum)
titel_figuur = "Zuurtegraad met gemiddeldes (+ MKN in blauw)"

## figuur 
figuur2a <- data %>% 
    filter(AquaComponent == "pH_veld") %>% 
    ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = FieldSampleID)) + 
    geom_line() +
    geom_point() +
  geom_hline(yintercept = MKN_pH_max, colour = "blue", linetype = "dashed") +
  geom_hline(yintercept = MKN_pH_min, colour = "blue", linetype = "dashed") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y")
figuur2a

```

Alle N-variabelen cumulatief in figuur zetten (Nitriet, nitraat, ammonium, OrgN = TN)
Aandeel van gemeten vennen voldoet aan MKN-normen (voor alle variabelen)

```{r}
# subset met nitraten (NO2, NO3, NH4)
nitraten <- subset(data, AquaComponent %in% c("NO2", "NO3", "NH4", "NH4_N", "NO2_N", "NO3_N"))
nutriënten <- subset(data, AquaComponent %in% c("NO2", "NO3", "NH4", "NH4_N", "NO2_N", "NO3_N", "TP", "N_anorg"))
nutriënten_R <- subset(nutriënten, FieldSampleID == "WVLDIK0133_R")
nutriënten_ZP <- subset(nutriënten, FieldSampleID == "WVLDIK0133_ZP")
```



# democode functie 'maak_figuur' 
functie: maak figuur, laat deze zien en sla ze op
```{r}
# genereer plot en sla op
save_plot <- function(dataset, component, naam, title = NULL, xas = NULL, yas = NULL, naam_linetype = NULL, labels_linetype = NULL){
  figuur <- dataset %>%
    filter(AquaComponent %in% component) %>%  
    ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = label, linetype = FieldSampleID)) +
    scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") + # visualiseert data per maand in format maand-jaar
    geom_line(size = 1) +
    geom_point() +
    labs(title = title) + xlab(xas) + ylab(yas) +
  scale_linetype_discrete(name = naam_linetype, labels = labels_linetype)
  print(figuur)
  ggsave(plot = figuur,
         filename = paste0("./exports/NKP_", naam, ".jpg"), width = 9, height = 6)
}

# puur om plot te genereren
maak_plot <- function(dataset, component, title = NULL, xas = NULL, yas = NULL, naam_linetype = NULL, labels_linetype = NULL){
  figuur <- dataset %>%
    filter(AquaComponent %in% component) %>%  
    ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = label, linetype = FieldSampleID)) +
    scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") + # visualiseert data per maand in format maand-jaar
    geom_line(size = 1) +
    geom_point() +
    labs(title = title) + xlab(xas) + ylab(yas) +
  scale_linetype_discrete(name = naam_linetype, labels = labels_linetype)
  figuur
}

# plots genereren voor 1 variabele (trendlijn randzone + trendlijn zonnepanelen)
maak_plot_1var <- function(dataset, component, title = NULL, xas = "Datum staalname", yas = NULL){
  figuur <- dataset %>%
    filter(AquaComponent %in% component) %>%  
    ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = FieldSampleID)) +
    scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") + # visualiseert data per maand in format maand-jaar
    geom_line(size = 1) +
    geom_point() +
    labs(title = title) + xlab(xas) + ylab(yas) +
  scale_color_discrete(name = "Locatie", labels = c("Randzone", "Zonnepanelen"))
  figuur
}
```

# meetreeks veldmetingen
```{r}
# meetreeks veldmetingen per plas
componenten_veld <- c("O2_veld", "O2verz", "sal_veld", "T_veld",
                                       "pH_tit", "pH_veld")
#veldmetingen <- data %>%
  filter(AquaComponent %in% componenten_veld) %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = label, linetype = FieldSampleID)) +
  geom_line() +
  geom_point() +
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  labs(title = "Veldmetingen") +
  xlab("Datum staalname") + 
  ylab("Meetwaarde (log10)")  + 
  scale_color_discrete("Variabelen") +
  scale_linetype_discrete("Locatie") +
  scale_y_log10()
veldmetingen
```


# democode: boxplots per variabele
afzonderlijke figuren per variabele
```{r warning=FALSE}
#Componenten <- unique(data$AquaComponent) 

#for (i in Componenten) { 
#  data_figuur <- data %>% 
#    filter(str_detect(AquaComponent, i)) 
#  figuur <- ggplot(data_figuur, 
#                   aes(x = FieldSampleID, y = ResultCalc)) + 
#    geom_boxplot() +
#    coord_flip() +
#    labs(title = paste0(data_figuur$label))
#  print(figuur)
#  ggsave(figuur, 
#         filename = paste0("./exports/plot_meetreeks_", "naam","_", i,".jpg"), 
#         dpi = 200, width = 10, height = 10)
#}
# geeft boxplots voor data per variabele, voor WVLDIK0133_ZP en WVLDIK0133_R
```

# Zuurtegraad

```{r}
data %>%
  filter(AquaComponent %in% "pH_veld") %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = FieldSampleID)) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
    geom_line() +
    geom_point() +
    labs(title = "Zuurtegraad (+ MKN maximum en minimum)") + xlab("Datum staalname") + ylab("Meetwaarde") +
  scale_color_discrete(name = "Locatie", labels = c("Randzone", "Zonnepanelen")) + geom_hline(yintercept = 6.5, color = "black", linetype = "dashed") +
  geom_hline(yintercept = 8.5, color = "black", linetype = "dashed")
  

data %>% filter(AquaComponent %in% "pH_veld") %>%
  summarise(maximum = max(ResultCalc), minimum = min (ResultCalc))
## max = 9.47 > 8.5 MKN - NOK
## min = 7.783 > 6.5 MKN - OK
```

# chlorofyl, zwevende stof & doorzicht

```{r}
# maak figuur
maak_plot(data, c("chl_a", "SD_veld", "ZS_550", "ZS_105"), "Helderheid waterkolom", xas = "Datum staalname", yas = "Meetwaarde", naam_linetype = "Locatie", labels_linetype = c("Randzone", "Zonnepanelen"))

## log-getransformeerd
maak_plot(data, c("chl_a", "SD_veld", "ZS_550", "ZS_105"), "Helderheid waterkolom", xas = "Datum staalname", yas = "Meetwaarde (log10)", naam_linetype = "Locatie", labels_linetype = c("Randzone", "Zonnepanelen")) +
  scale_y_log10() +
  scale_color_discrete(name = "Variabelen")

# afzonderlijke plots

## chlorofyl-a
maak_plot(data, "chl_a", "Chlorofyl-a concentratie", xas = "Datum staalname", yas = "Meetwaarde (µg/L)", naam_linetype = "Locatie", labels_linetype = c("Randzone", "Zonnepanelen"))
data %>% 
  filter(AquaComponent %in% "chl_a") %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = FieldSampleID)) +
    scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
    geom_line() +
    geom_point() +
    labs(title = "Chlorofyl-a concentratie") + xlab("Datum staalname") + ylab("Meetwaarde (µg/L)") +
  scale_color_discrete(name = "Locatie", labels = c("Randzone", "Zonnepanelen"))
  
  

## Secchi-diepte
data %>%
  filter(AquaComponent %in% "SD_veld") %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc)) +
  geom_line() +
  geom_point() +
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  labs(title = "Doorzicht waterkolom (+ MKN zomerhalfjaargemiddelde)") + xlab("Datum staalname") + ylab("Secchi-diepte (m)") +
  geom_hline(yintercept = 1.8, color = "black", linetype = "dashed")

data %>%
  filter(AquaComponent %in% "SD_veld") %>%
  filter(FieldSamplingDate >= ymd("2024-04-21") & FieldSamplingDate < ymd("2024-09-21")) %>%
  summarise(zomerhalfjaar = mean(ResultCalc))
## Zomerhalfjaargemiddelde = 1.89 m > 1.8 m (MKN) - OK

## Zwevende stof
maak_plot(data, c("ZS_105", "ZS_550"), "Zwevende stoffen in suspensie", xas = "Datum staalname", yas = "Meetwaarde (g/L)")
### ZS_105 toename in september (fytplankton?)
```

# nutriëntenlast
```{r}
data %>%
  filter(AquaComponent %in% c("N_anorg", "NO2_N", "NO3_N", "NH4_N")) %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc, color = label, linetype = FieldSampleID)) + 
  geom_line() + 
  geom_point() +
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  labs(title = "Anorganische stikstof NKP") +
  xlab("Datum staalname") + 
  ylab("Meetwaarde (mg N/L)")  + 
  scale_color_discrete("Variabelen") +
  scale_linetype_discrete("Locatie")
# zonder fosfor

# afZonderlijke plots

## Ammonium-N
maak_plot_1var(data, "NH4_N", "Ammonium-N-concentratie", xas = "Datum staalname", yas = "Meetwaarde (mg N/L)")

## Nitriet-N
maak_plot_1var(data, "NO2_N", "Nitriet-N-concentratie", xas = "Datum staalname", yas = "Meetwaarde (mg N/L)")

## Nitraat-N
maak_plot_1var(data, "NO3_N", "Nitraat-N-concentratie", xas = "Datum staalname", yas = "Meetwaarde (mg N/L)")

## Anorganische stikstof
maak_plot_1var(data, "N_anorg", "Anorganische stikstof (NH4_N + NO3_N + NO2_N)", xas = "Datum staalname", yas = "Meetwaarde (mg N/L)")
### zomerhalfjaargemiddelde anorganische stikstof
data %>%
  filter(AquaComponent %in% "N_anorg") %>%
  filter(FieldSamplingDate >= ymd("2024-04-21") & FieldSamplingDate < ymd("2024-09-21")) %>%
  summarise(zomerhalfjaar = mean(ResultCalc))
### 0.317 mg/L < 1.3 mg/L MKN - OK
### zonder organische stikstof!
### hernoem naar som ammonium, nitriet, nitraat (=som anorganische stikstof)


## Totale fosfor
maak_plot(data, "TP", "Totale fosfor-concentratie", xas = "Datum staalname", yas = "Meetwaarde (mg P/L)", naam_linetype = "Locatie", labels_linetype = c("Randzone", "Zonnepanelen"))
### zomerhalfjaargemiddelde TP
data %>%
  filter(AquaComponent %in% "TP") %>%
  filter(FieldSamplingDate >= ymd("2024-04-21") & FieldSamplingDate < ymd("2024-09-21")) %>%
  summarise(zomerhalfjaar = mean(ResultCalc))
### 0.171 mg/L >> 0.055 mg/L MKN - NOK
### voeg MKN toe

```

## cumulatieve plot anorganische stikstof
```{r}
# randzone
plot1 <- data %>%
  filter(FieldSampleID == "WVLDIK0133_R" & AquaComponent %in% c("NH4_N","NO3_N", "NO2_N")) %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc, fill = label)) +
  geom_area() +
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  labs(title = "Randzone") +
  xlab("Datum staalname") + 
  ylab("Meetwaarde (mg N/L)")  + 
  scale_fill_discrete("Variabelen")

# zonnepanelen
plot2 <- data %>%
  filter(FieldSampleID == "WVLDIK0133_ZP" & AquaComponent %in% c("NH4_N","NO3_N", "NO2_N")) %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc, fill = label)) +
  geom_area() +
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  labs(title = "Zonnepanelen") +
  xlab("Datum staalname") + ylab(NULL)

# gecombineerde figuur
plot3 <- ggarrange(plot1, plot2, common.legend = TRUE, legend = "bottom")

annotate_figure(plot3, top = text_grob("Anorganische stikstof (NH4_N + NO3_N + NO2_N)", 
               color = "black", face = "bold", size = 14))

maak_plot_1var(data, "N_anorg", "Anorganische stikstof (NH4_N + NO3_N + NO2_N)", yas = "Meetwaarde (mg N/L")
```

# zuurstof
```{r}
maak_plot(data, c("O2verz", "O2_veld"), "Zuurstofgehalte", xas = "Datum staalname", yas = "Meetwaarde", naam_linetype = "Locatie", labels_linetype = c("Randzone", "Zonnepanelen"))

# afzonderlijke plots

## Zuurstofverzadiging
maak_plot_1var(data, "O2verz", "Zuurstof-verzadiging (+ MKN norm maximum)", xas = "Datum staalname", yas = "Meetwaarde (%)") +
  geom_hline(aes(yintercept= 120), colour= 'black', linetype = "dashed")
data %>% filter(AquaComponent %in% "O2verz") %>%
  summarise(maximum = max(ResultCalc))
### 161.9 % >> 120% MKN - NOK

## Zuurstofconcentratie
maak_plot_1var(data, "O2_veld", "Zuurstof-concentratie (+ 10-percentiel MKN)", xas = "Datum staalname", yas = "Meetwaarde (mg/L)")+ geom_hline(aes(yintercept= 6), colour= 'black', linetype = "dashed")
quantile(data$ResultCalc[data$AquaComponent %in% "O2_veld"], probs = 0.1)
# p-10 = 8.731 > 6 mg/L  - OK
```

# temperatuur
```{r}
maak_plot_1var(data, "T_veld", "Temperatuur waterkolom (+ maximum MKN)", xas = "Datum staalname", yas = "Meetwaarde (° C)") +
  geom_hline(yintercept = 25, color = "black", linetype = "dashed")
data %>% filter(AquaComponent %in% "T_veld") %>%
  summarise(maximum = max(ResultCalc))
### Maximum = 23.5 °C < 25 °C MKN - OK
```

# silicium
```{r}
maak_plot_1var(data, "Si", "Silicium-concentratie", xas = "Datum staalname", yas = "Meetwaarde (mg/L)")

## anorganische stikstof + silicium
plot1 <- data %>%
  filter(FieldSampleID == "WVLDIK0133_R" & AquaComponent %in% c("N_anorg", "NH4_N","NO3_N", "NO2_N")) %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc, fill = label)) +
  geom_area() +
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  labs(title = "Randzone") +
  xlab("Datum staalname") + 
  ylab("Meetwaarde (mg N/L)")  + 
  scale_fill_discrete("Variabelen") + geom_line(data = data %>% filter(AquaComponent == "Si" & FieldSampleID == "WVLDIK0133_R") , aes(x = FieldSamplingDate, y = ResultCalc, linetype = FieldSampleID)) + scale_linetype_discrete(name = NULL, labels = "Silicium")

# zonnepanelen
plot2 <- data %>%
  filter(FieldSampleID == "WVLDIK0133_ZP" & AquaComponent %in% c("N_anorg", "NH4_N","NO3_N", "NO2_N")) %>%
  ggplot(aes(x = FieldSamplingDate, y = ResultCalc, fill = label)) +
  geom_area() +
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  labs(title = "Zonnepanelen") +
  xlab("Datum staalname") + ylab(NULL) + geom_line(data = data %>% filter(AquaComponent == "Si" & FieldSampleID == "WVLDIK0133_ZP") , aes(x = FieldSamplingDate, y = ResultCalc, linetype = FieldSampleID)) + scale_linetype_discrete(name = NULL, labels = "Silicium")

# gecombineerde figuur
plot3 <- ggarrange(plot1, plot2, common.legend = TRUE, legend = "bottom")

annotate_figure(plot3, top = text_grob("Anorganische stikstof (N_anorg = NH4_N + NO3_N + NO2_N) + Silicium", 
               color = "black", face = "bold", size = 14))
```

# zoutparameters
```{r}
# zout-ionen
maak_plot(data, c("SO4", "SO4_S", "Cl", "Na"), "Zout-ionen", xas = "Datum staalname", yas = "Meetwaarde (mg/L)", naam_linetype = "Locatie", labels_linetype = c("Randzone", "Zonnepanelen"))

# afzonderlijke plots

## Sulfaat
maak_plot_1var(data, "SO4", "Sulfaat-concentratie (+ MKN 90-percentiel)", xas = "Datum staalname", yas = "Meetwaarde (mg/L)") + geom_hline(yintercept = 100, color = "black", linetype = "dashed", size = 1) + ylim(0,110)
quantile(data$ResultCalc[data$AquaComponent %in% "SO4"], probs = 0.9)
## 90p = 49.815 mg/L (<< MKN 100 mg/L) OK

## Sulfaat-Sulfur
maak_plot_1var(data, "SO4_S", "Sulfaat-S-concentratie", xas = "Datum staalname", yas = "Meetwaarde (mg/L)")

## Chloride
maak_plot_1var(data, "Cl", "Chloride-concentratie (+ MKN 90-percentiel)", xas = "Datum staalname", yas = "Meetwaarde (mg/L)") + geom_hline(yintercept = 140, color = "black", linetype = "dashed")
quantile(data$ResultCalc[data$AquaComponent %in% "Cl"], probs = 0.9)
## 90p = 158.19 mg/L (> MKN 140 mg/L) NOK

## Natrium
maak_plot_1var(data, "Na", "Natrium-concentratie", xas = "Datum Staalname", yas = "Meetwaarde (mg/L)")

# saliniteit 
maak_plot_1var(data, "sal_veld", "Saliniteit", xas = "Datum staalname", yas = "Meetwaarde (g/kg)")

# Elektrische geleidbaarheid
maak_plot_1var(data, c("EGV25_veld"), "Elektrische geleidbaarheid (+ MKN 90-percentiel)", xas = "Datum staalname", yas = "Meetwaarde (µS/cm)") + geom_hline(yintercept = 750, color = "black", linetype = "dashed")
quantile(data$ResultCalc[data$AquaComponent %in% "EGV25_veld"], probs = 0.9)
## 90p = 922.4 µS/cm (> MKN 750 µS/cm 20°C ) NOK

```

# Maucha-diagram

## Functie Maucha-diagram (mymaucha)
```{r}
library(utils)
mymaucha <- function(x, col = NULL,
                   labels = TRUE, labels.col = 1,
                   polygon = FALSE, polygon.col = 1,
                   main = NULL, ...){
  
  if(is.null(col)){
    col <- c("#54c7fd", "#ffcd00", "#ff9600", "#ff2851",
             "#0076fe", "#41cd58", "#ff3823", "#8f8e94")
  }
  col <- rep_len(col, length.out = 8)
  polygon.col <- rep_len(polygon.col, length.out = 8)
  
  if(is.vector(x)){
    n.plot <- 1
    x.all <- t(as.matrix(x))
  } else {
    x.all <- as.matrix(x)
    n.plot <- nrow(x.all)
    if(is.null(main)){
      main <- row.names(x.all)
    }
  }
  
  par.mar.0 <- par("mar")
  par.mfrow.0 <- par("mfrow")
  par(mfrow = c(.dimMosaic(n.plot)[1], .dimMosaic(n.plot)[2]),
      mar = c(2, 0, 2, 0))
  
  for(j in 1:n.plot){
    
    x <- x.all[j, ]
    
    x[1] <- (x[1] * 1) / 39.0983
    x[2] <- (x[2] * 1) / 22.9898
    x[3] <- (x[3] * 2) / 40.0780
    x[4] <- (x[4] * 2) / 24.3050
    x[5] <- (x[5] * 2) / 96.0626
    x[6] <- (x[6] * 1) / 35.4530
    x[7] <- (x[7] * 1) / 61.0168
    x[8] <- (x[8] * 2) / 60.0089
    
    x[1:4] <- x[1:4] / sum(x[1:4]) * 100
    x[5:8] <- x[5:8] / sum(x[5:8]) * 100
    
    A  <- sum(x)
    R <- sqrt(((A / 16) * 2 / sin(22.5 * pi / 180)))
    a <- x / (R * sin(22.5 * pi / 180))
    
    
    coord.x <- matrix(NA, nrow = 5, ncol = 8)
    coord.y <- matrix(NA, nrow = 5, ncol = 8)
    coord.x[1, ] <- coord.x[5, ] <- rep(0, 8)
    coord.y[1, ] <- coord.y[5, ] <- rep(0, 8)
    coord.x[2, ] <- cos(seq(90, -225, -45) * pi / 180) * R
    coord.y[2, ] <- sin(seq(90, -225, -45) * pi / 180) * R
    coord.x[3, ] <- cos(seq(67.5, -247.5, -45) * pi / 180) * a
    coord.y[3, ] <- sin(seq(67.5, -247.5, -45) * pi / 180) * a
    coord.x[4, ] <- cos(seq(45, -270, -45) * pi / 180) * R
    coord.y[4, ] <- sin(seq(45, -270, -45) * pi / 180) * R
    
    
    if(polygon | labels){
      coord.poly.x <- coord.x
      coord.poly.x[3, ] <- cos(seq(67.5, -247.5, -45) * pi / 180) * R
      coord.poly.y <- coord.y
      coord.poly.y[3, ] <- sin(seq(67.5, -247.5, -45) * pi / 180) * R
    }
    
    if(labels){
      lab <- c(expression("K"^"+"), expression("Na"^"+"), expression("Ca"^"2+"), expression("Mg"^"2+"), 
               expression("SO"["4"]^"2-"), expression("Cl"^"-"), expression("HCO"["3"]^"-"), expression("CO"["3"]^"2-"))
      lab.pos.x <- coord.x[3, ]
      lab.pos.y <- coord.y[3, ]
      lab.pos.x[a < R] <- coord.poly.x[3, a < R]
      lab.pos.y[a < R] <- coord.poly.y[3, a < R]
      lab.pos.x <- lab.pos.x + cos(seq(67.5, -247.5, -45) * pi / 180) * 2
      lab.pos.y <- lab.pos.y + sin(seq(67.5, -247.5, -45) * pi / 180) * 2
    }
    
    
    xlim <- max(abs(c(min(lab.pos.x), max(lab.pos.x))))
    ylim <- max(abs(c(min(lab.pos.y), max(lab.pos.y))))
    plot(0, 0, type = "n", xlim = c(-xlim, xlim), ylim = c(-ylim, ylim),
         xaxt = "n", yaxt = "n", xlab = "", ylab = "", asp = 1, bty = "n", main = main[j], ...)
    
    for(i in 1:8){
      polygon(x = coord.x[, i], y = coord.y[, i], col = col[i], border = NA)
      if(polygon){
        lines(x = coord.poly.x[, i], y = coord.poly.y[, i], col = polygon.col[i])
      }
    }
    
    if(labels){
      text(lab.pos.x, lab.pos.y, lab, col = labels.col)
    }
  }
  par(mar = par.mar.0, mfrow = par.mfrow.0)
}
```
## Subfunctie .dimMosaic
```{r}
.dimMosaic <- function(n.plot){
  x <- floor(sqrt(n.plot))
  y <- floor(sqrt(n.plot))
  if(x * y < n.plot) y <- y + 1
  if(x * y < n.plot) x <- x + 1
  return(c(x, y))
}
```

## Maak dataset met ionen
```{r}
# dataset met ionen
ionen <- data %>%
  filter(AquaComponent %in% c("K", "Na", "Ca", "Mg", "SO4", "Cl", "HCO3_OEP", "CO3")) %>%
  group_by(AquaComponent, FieldSamplingDate) %>%
  summarise(NKP = mean(ResultCalc))
write.csv2(ionen, file = "./data/IonenNKP.csv")
# to wide format
ionenNKP <- read_csv2("./data/IonenNKP.csv")[2:9] # laat fieldsamplingdate vallen

```

## Maak diagrammen aan
```{r}
## CO3^2- = CO3
## HCO3- = HCO3_OEP
maucha(ionen_NKP, main = "Ionensamenstelling Nieuwkappelleplas \n oktober 2023-augustus 2024")
maucha(ionen_NKP, polygon = TRUE)
mymaucha(ionenNKP, main = "Ionensamenstelling Nieuwkappelleplas \n oktober 2023-augustus 2024")
```
